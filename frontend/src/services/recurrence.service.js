import { api } from './api.js'
export const recurrenceService = { async list(status = 'active') { return (await api.get('/recurrences', { params: { status } })).data.recurrences }, async create(data) { return (await api.post('/recurrences', data)).data.recurrence }, async update(id, data) { return (await api.patch(`/recurrences/${id}`, data)).data.recurrence }, async generate(through) { return (await api.post('/recurrences/generate', { through })).data } }
export const subscriptionService = { async list(status = 'active') { return (await api.get('/subscriptions', { params: { status } })).data }, async create(data) { return (await api.post('/subscriptions', data)).data.subscription }, async update(id, data) { return (await api.patch(`/subscriptions/${id}`, data)).data.subscription } }
